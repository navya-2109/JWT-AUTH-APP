# 🔐 JWT Authentication App (Express.js + Frontend)

A simple and clean **JWT-based authentication system** built with **Node.js, Express**, and a **vanilla JavaScript frontend**.  
It demonstrates how to register users, log in, generate and verify JWT tokens, and access protected routes securely.

---

## 🚀 Features

- ✅ User login with JWT token generation  
- ✅ Secure backend token verification  
- ✅ Protected route accessible only with valid token  
- ✅ Frontend stores token in browser’s localStorage  
- ✅ Clean, card-style login UI  
- ✅ Fully functional Express backend with `users.json` file  

---

## 🗂️ Project Structure

